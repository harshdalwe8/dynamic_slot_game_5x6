import React from 'react';

const SlotGrid: React.FC = () => {
    // This component is deprecated - using SlotMachine instead
    return (
        <div style={{ padding: '20px', textAlign: 'center' }}>
            <p>SlotGrid component - Please use SlotMachine component instead</p>
        </div>
    );
};

export default SlotGrid;